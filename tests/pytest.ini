# pytest.ini
[pytest]
env =
    CENTML_CACHE_DIR=/tmp/centml-test
filterwarnings = 
    ignore::UserWarning
    ignore:.*use (CentMLClient.)?fetch_logs.. instead:DeprecationWarning
markers = 
    gpu: this test needs the gpu to run quickly

